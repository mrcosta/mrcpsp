executionType=ALL_TIMES
#ONE_FILE
#ONE_FILE_TIMES
#ALL
#ALL_TIMES
#READ_PSPLIB_INSTANCE

executionTimes=20

thread=0
# 1 or 0 -- 1 == true, 0 == false

instanceFolder=/data/j10.mm/
#instance.folder=/data/j20.mm/
#instance.folder=/data/psplibresults/

#instance.file=j30hrs.mm
instanceFile=j1012_1.mm

concurrentPoolSize=8

executeLocalSearch=1
# 1 or 0 -- 1 == true, 0 == false

typeLocalSearch=BSFM
#LNRC = lower non renewable consumption
#LNRCCP = lower non renewable consumption critical path
#BSFM = jobs block with shortest feasible mode
#BRNRC= best ratio non renewable comsumption
#BRRC = best ratio renewable comsumption
#BRC = best ratio comsumption

generateDiagram=0
diagramPath=gantt
showPredecessors=0
# 1 or 0 -- 1 == true, 0 == false

writeLowerboundForAllInstances=0
# 1 or 0 -- 1 == true, 0 == false
# this one is slightly used

showCriticalPath=0
# 1 or 0 -- 1 == true, 0 == false

showLowerBound=0
# 1 or 0 -- 1 == true, 0 == false

testName=j10.json

testDescription=basic tests

